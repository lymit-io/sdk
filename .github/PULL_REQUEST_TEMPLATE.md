## What

## Why

## Checks

- [ ] Tests added or updated
- [ ] `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` pass
- [ ] Docs updated (architecture / data model / API contract) if behaviour changed
- [ ] Changeset added (`pnpm changeset`) if `@lymit/sdk` changed
